services:
  db:
    image: postgres:15
    container_name: tren_wisuda_db
    environment:
      POSTGRES_DB: db_tren_wisuda
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      # Standardized: project 3 → port 5003
      - "5003:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d db_tren_wisuda"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    container_name: tren_wisuda_backend
    environment:
      PGHOST: db
      PGPORT: 5432
      PGDATABASE: db_tren_wisuda
      PGUSER: postgres
      PGPASSWORD: ${POSTGRES_PASSWORD}
      CORS_ORIGINS: http://localhost:3003
    ports:
      # Standardized: project 3 → port 8003
      - "8003:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./history_peserta_wisuda:/app/history_peserta_wisuda
    command: >
      sh -c "python setup_db.py && 
             python -m uvicorn main:app --host 0.0.0.0 --port 8000"

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_BASE: http://localhost:8003
    container_name: tren_wisuda_frontend
    environment:
      NEXT_PUBLIC_API_BASE: http://localhost:8003
    ports:
      # Standardized: project 3 → port 3003
      - "3003:3000"
    depends_on:
      - backend

volumes:
  postgres_data:
