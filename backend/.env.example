# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGDATABASE=history_peserta_wisuda
PGUSER=postgres
PGPASSWORD=postgres

# Optional: DATABASE_URL overrides PG* settings
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/history_peserta_wisuda

# CORS
CORS_ORIGINS=http://localhost:3000
